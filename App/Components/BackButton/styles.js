import {StyleSheet} from 'react-native'

export default StyleSheet.create({
  backButtonContainer: {
    width: 40,
    alignItems: 'center',
    justifyContent: 'center'
  },
  backIcon: {
    padding: 10
  }
})
